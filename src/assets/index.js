//clicking numbers
const numberDiv=document.querySelectorAll(".number")
numberDiv.forEach(element => {
    element.addEventListener("click",()=>{
       const numberH1=console.log(element.querySelector("h1"));
       array.forEach(element => {
        
       });
      

    })
    
});

